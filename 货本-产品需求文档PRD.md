# 货本 · 个人卖家轻量级出入库管理工具
## 产品需求文档 (PRD)

| 版本 | 日期 | 状态 | 编写/更新说明 |
|---|---|---|---|
| v1.0 | 2026-08-19 | 已废弃 | 网页/PWA 原型阶段初步草案 |
| v1.1 | 2026-08-20 | 已归档 | 原生 Android 架构设计与功能迁移 |
| v1.2 | 2026-08-20 | 已归档 | 对齐 Android 原生工程实现（多尺码/扫码枪/ML Kit/CSV导入导出/原子存储） |
| **v1.3** | **2026-08-20** | **正式发布** | **全量架构治理与体验重构（P0图片存储解耦/并发Mutex加锁/P1导航栈/库存页3行Header/烟花特效/SAF备份）** |

---

## 1. 产品背景与定位

### 1.1 核心痛点
个人卖家与轻量小微商家（闲鱼、抖音小店、微信私域、小红书、线下档口/展会转卖）在日常进出货与库存管理中面临以下问题：
- **货位混乱**：商品随处堆放，找货全靠翻找记忆。
- **库存盲区**：无法实时掌握各品类/尺码准确余量，易造成断货或超卖。
- **账目分散**：进货成本、实际售价、出货渠道分散在聊天记录或脑中，利润算不清。
- **传统软件过重**：市面进销存系统多面向中大型企业，权限多、流程繁琐、配置门槛高且收费昂贵。

### 1.2 产品定位
**货本（StockMaster）** 是一款面向个人及微型卖家的**极速、单机离线可用、全功能出入库记账工具**。  
核心设计原则是 **「扫码即记、秒级出入、账货清晰、数据私有」**。

---

## 2. 目标用户与使用场景

| 用户类型 | 典型特征 | 核心诉求 |
|---|---|---|
| **二手/闲置转卖卖家**（闲鱼等） | SKU 杂、单品数量少、条码多变 | 极速扫码建档、找货定库位、记录销售成交价 |
| **服饰/鞋帽小微卖家** | 单品存在多尺码/配比规格 | 尺码矩阵库存管理、批量配比出入库、缺码预警 |
| **数码配件/日用百货卖家** | 走量较大、有固定进销差价 | 扫码枪连扫入库、出入库流水追溯、利润毛利自动核算 |
| **兼职/副业档口卖家** | 时间碎片化、无专职记账人员 | 手机即开即用、无需联网登录、全量备份/表格随时导出对账 |

---

## 3. 产品核心目标与体验指标

1. **秒级出入库**：常规扫码识别到确认提交 **≤ 3 秒**，扫码成功后自动展示满屏烟花特效并无缝跳转至商品档案详情页。
2. **多模态扫码支持**：同时支持 **手机摄像头（ML Kit，响应式权限开摄）**、**外接 USB/蓝牙硬件扫码枪**、**相册条码图片识别** 与 **手动输入**。
3. **数据资产私有与解耦**：离线优先，图片脱敏存储至私有 `.jpg` 文件；持久化采用协程 `Mutex` 串行互斥加锁与原子重命名策略；支持全量 JSON 备份及无损 CSV 导入导出。
4. **指标直观透明**：总资产、总件数、本月毛利、低库存警报一屏全览。

---

## 4. 功能架构全景

```
货本 StockMaster (Android Native)
├── 1. 仪表盘 (Dashboard)
│   ├── 总库存价值 (¥) 与单品平均价值
│   ├── 总在库件数与品类总量
│   ├── 本月销售出库毛利核算 (+¥)
│   ├── 低库存缺货预警快捷看板
│   ├── 扫码入库 / 扫码出库快捷工作台入口
│   └── 最近出入库动态流水快照
│
├── 2. 扫码工作台 (Scan Workbench)
│   ├── CameraX + Google ML Kit 实时视觉对焦识别（响应式权限处理，授权即刻开摄）
│   ├── 动态激光扫描动画与四角精密工业角标
│   ├── 物理闪光灯开关 / 前后摄翻转 / 相册选图识别 / 手动输入弹窗
│   ├── 外接扫码枪全局监听（与输入法焦点智能解耦）
│   ├── 扫码结果抽屉 (ScanResultDrawer)：
│   │   ├── 已建档商品识别展示
│   │   ├── 未识别条码引导一键建档录入
│   │   ├── 数量步进器 + 快捷梯度 (+1/+5/+10/+50/+100)
│   │   ├── 进价/售价快速微调、存放库位选择、事由备注
│   │   └── 多尺码选择 / 多码批量配比模式 (Batch Size Breakdown)
│   ├── 扫码成功满屏烟花粒子连发爆裂特效 (ConfettiFireworksEffect)
│   ├── 声音与触觉反馈 (BeepPlayer: 扫码/成功/警报音频 + Haptic)
│   └── 扫码完成自动带参跳转至商品档案详情页
│
├── 3. 库存管理 (Inventory)
│   ├── 3 行高密度紧凑 Header：
│   │   ├── Row 1：主标题 + 统计胶囊 + 右侧「分类库位」与「+ 录入商品」快捷按钮
│   │   ├── Row 2：搜索框与库位下拉筛选单行并排
│   │   └── Row 3：横向单行滑轨整合「全部」、「缺货预警」及分类胶囊
│   ├── 全局模糊搜索（商品名 / SKU / 条形码 / 库位）
│   ├── 商品列表卡片（缩略图、SKU、库位、售价、库存容量进度条、低库存徽标）
│   ├── 列表快捷入库/出库弹窗 (QuickTransactionDialog)
│   ├── 录入新商品 (AddProductScreen)
│   └── 分类与库位维护管理 (CategoryLocationScreen)
│
├── 4. 商品详情与编辑 (ProductDetail & Edit)
│   ├── 商品全景信息：分类标签、多尺码标签、SKU、条码、库位
│   ├── 核心指标面板：当前在库、单位成本、单位售价
│   ├── 库存容量上限百分比与预警状态指示
│   ├── CODE128 高清条码位图生成（供贴标打印/现场校验）
│   ├── 多尺码库存矩阵分布柱状指示
│   ├── 该商品专属历史流水时间线
│   ├── 快捷入库/出库操作面板
│   ├── 缩略图点击全屏捏合手势放大查看器 (PhotoViewerDialog)
│   └── 档案编辑（全字段修改/多尺码配置）与删除（防误删确认弹窗）
│
├── 5. 出入库流水 (History)
│   ├── 统计卡片：总入库件数/金额、总出库件数/金额
│   ├── 维度筛选：全部流水 / 仅入库 / 仅出库
│   ├── 时间筛选：自然月份下拉切换 (如 2026年 8月)
│   ├── 日期时间线智能分组展示（今天、昨天、具体日期）
│   └── 流水明细卡片（类型标识、数量增减、变动总额、业务事由、时间、操作库位）
│
├── 6. 全局检索 (Global Search)
│   └── 跨品类、跨条码、跨库位的毫秒级联想搜索与结果列表直接跳转详情
│
└── 7. 设置与数据管理 (Settings)
    ├── 标准化 SAF 系统文件选择器 (CreateDocument / OpenDocument)
    ├── 数据导出：全量数据备份 (JSON)、商品档案表格 (CSV)、出入库流水表格 (CSV)
    ├── 数据导入：全量数据恢复 (JSON)、商品档案表格 (CSV)、出入库流水表格 (CSV)
    ├── 分类与库位维护快捷入口
    └── 清空全部数据（二次危险确认）
```

---

## 5. 详细功能需求

### 5.1 扫码出入库系统
1. **摄像头扫描**：
   - 依赖 CameraX 与 ML Kit Barcode Scanning，支持 EAN-13、EAN-8、Code 128、Code 39、UPC-A、UPC-E、QR Code 等主流编码。
   - 包含防抖与防重复识别机制（1200ms 同码过滤）。
   - 响应式相机权限处理：首次弹出系统授权弹窗，用户点击“允许”后即刻拉起 PreviewView，无需退屏重进。
2. **硬件扫码枪适配**：
   - 全局拦截按键事件，按键间隔小于 120ms 且以 Enter 结尾判定为扫码枪输入。
   - 当页面处于文本框编辑输入状态时，自动放行事件，不干扰键盘打字。
3. **识别结果分支与后续动作**：
   - **已存在商品**：弹出抽屉自动带出当前库存、商品图、进价/售价与预设库位；确认后播放 Beep 提示音，全屏爆炸式连发 **彩带烟花特效 (`ConfettiFireworksEffect`)**，并在 1.6 秒后（或点击「查看商品详情 ➔」）**自动跳转至该商品的档案详情页**。
   - **未录入条码**：提示「发现新条形码」，展示条码内容，提供「为该条码录入新商品」按钮直接进入预填条码的建档页面。
4. **多尺码操作**：
   - 单码操作：选择对应尺码，显示该尺码当前余量。
   - 批量配比（Batch Size Scan）：可同时输入 S/M/L 各自的增减数量，系统自动校验总数并扣减。

### 5.2 商品档案管理与图片解耦
1. **基本字段**：商品名称（必填）、SKU 编码（默认条形码 ID，无强加前缀）、条形码、商品分类、存放库位、计量单位、单位成本 (¥)、单位售价 (¥)、商品备注。
2. **库存与容量控制**：初始库存、预警阈值（`minStock`）、存放容量上限（`maxCapacity`，可选）。
3. **多尺码管理**：提供预设填充（`S-XXXL`、鞋码 `36-44` 等）与自定义增删改，总库存自动联动尺码库存之和。
4. **图片存储解耦与手势放大**：
   - 图片压缩并保存至应用私有目录 `context.filesDir/images/img_xxx.jpg`，模型中仅存储 `file://...` 路径。
   - 图片解码内存占用降低 **95%+**，彻底消除 JSON 膨胀与 OOM 隐患。
   - 点击商品图片可调起全屏查看器（`PhotoViewerDialog`），支持手势双指捏合缩放与拖拽位移。

### 5.3 基础数据与并发写保护
- 分类与库位支持新增、重命名级联同步及删除校验。
- 在 `MainViewModel` 中对所有磁盘写入逻辑采用 `Mutex` 协程互斥加锁（`itemPersistMutex`, `txPersistMutex`），确保在扫描枪高频连续扫码写入时强一致性与零数据竞态。

### 5.4 界面设计与交互规范
1. **沉浸式全屏**：采用 Edge-to-Edge 设计，系统状态栏与导航栏完全透明浸入背景。
2. **库存页 3 行 Header 重构**：
   - 彻底优化早期 5 行垂直堆叠占用（节省 155dp+ 垂直空间），首屏多展示 2~3 张完整商品卡片。
3. **设置页规范**：按钮统一移除冗余括号说明，纯粹聚焦核心功能。

### 5.5 数据交换与备份 (SAF + CSV)
1. **导出**：
   - **全量数据备份 (JSON)**：通过 SAF `CreateDocument("application/json")` 导出全量商品、流水、分类与库位备份包。
   - **商品档案表格 (CSV)**：导出 UTF-8 BOM 编码的 CSV 表格。
   - **出入库流水表格 (CSV)**：导出 UTF-8 BOM 编码的流水明细表格。
2. **导入与还原**：
   - 通过 SAF `OpenDocument()` 支持全量 JSON 备份包一键还原，或从外部 CSV 表格导入/更新商品档案与流水单据。
3. **备份策略**：去除了不可控的后台静默备份，完全由用户按需主动触发手动备份，数据 100% 自主可控。

---

## 6. 数据模型设计 (Data Schema)

### 6.1 商品档案模型 (`InventoryItem`)
```kotlin
@Serializable
data class InventoryItem(
    val id: String,                    // 唯一 UUID
    val sku: String,                   // SKU 编码
    val barcode: String,               // 条形码 / 二维码
    val name: String,                  // 商品名称
    val category: String,              // 所属分类
    val stock: Int,                    // 当前总库存量
    val minStock: Int,                 // 预警阈值（低于此值触发缺货预警）
    val maxCapacity: Int? = null,      // 库位容量上限（用于计算饱和度）
    val unitCost: Double,              // 进货成本单价 (元)
    val unitPrice: Double,             // 销售售价单价 (元)
    val location: String,              // 存放库位
    val imageUrl: String = "",         // 本地文件路径 file://... 或 legacy Base64
    val unit: String = "件",           // 计量单位 (件/个/箱/套等)
    val description: String = "",      // 规格/备注说明
    val hasSizes: Boolean = false,     // 是否启用多尺码
    val sizeVariants: List<SizeVariant> = emptyList(), // 尺码列表
    val updatedAt: String              // ISO8601 更新时间戳
)
```

### 6.2 备份数据包模型 (`BackupBundle`)
```kotlin
@Serializable
data class BackupBundle(
    val version: Int = 1,
    val appName: String = "StockMaster",
    val backupTime: String,
    val itemCount: Int,
    val transactionCount: Int,
    val items: List<InventoryItem>,
    val transactions: List<TransactionRecord>,
    val categories: List<String>,
    val locations: List<String>
)
```

---

## 7. 非功性需求与技术实现

| 维度 | 要求与实现方案 |
|---|---|
| **操作系统与兼容性** | Android 6.0 (API 23) 及以上，完美适配 Android 14/15 (Target SDK 35) |
| **UI 架构与设计语言** | 采用 Jetpack Compose + Material3，遵循沉浸式 Edge-to-Edge 设计，固定高质感 Light 主题 |
| **异步并发与互斥加锁** | 严格分离数据流，所有文件持久化 I/O 均由 Kotlin Coroutines `Dispatchers.IO` 异步调度，并由 `Mutex` 串行互斥保护，保证扫码写安全 |
| **存储安全性与图片解耦** | 采用 `.tmp` 临时文件 + 原子重命名（`renameTo`）写入策略；图片独立存入 `filesDir/images/`，数据模型轻量化 |
| **硬件扫码与动效反馈** | 扫码枪响应延迟 < 50ms；ML Kit 30fps 实时解析；扫码成功搭载自定义 Canvas 满屏烟花粒子动画 |
| **数据自主与无网络依赖** | 全功能本地单机运行，完全零网络依赖，数据 100% 保存在 App 私有目录与用户指定备份目录 |

---

## 8. 未来演进规划 (Roadmap)

- **v1.4（规划中）**：
  - 接入 ESC/POS 蓝牙热敏标签打印机协议，支持详情页直接蓝牙无线打印不干胶商品标签。
  - 支持多仓库独立核算（主仓、分仓切换）。
  - 本地加密数据包一键打包快传与跨设备备份。
