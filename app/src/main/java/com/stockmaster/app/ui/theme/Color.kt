package com.stockmaster.app.ui.theme

import androidx.compose.ui.graphics.Color

// ── 主题品牌色 ──────────────────────────────────────────────
// 翡翠绿 · 专业、干净、不刺眼
val GreenPrimary    = Color(0xFF007A50)   // 主色（按钮、图标、高亮）
val GreenDark       = Color(0xFF005236)   // 悬停/深色态
val GreenLight      = Color(0xFF34D399)   // 辅助亮色（徽章、成功）
val GreenTint       = Color(0xFFECFDF5)   // 浅底色
val GreenBorder     = Color(0xFFA7F3D0)   // 绿色边框

// 信号红 · 警示、出库
val RedPrimary      = Color(0xFFDC2626)
val RedDark         = Color(0xFFB91C1C)
val RedLight        = Color(0xFFFEE2E2)
val RedTint         = Color(0xFFFEF2F2)
val RedBorder       = Color(0xFFFCA5A5)

// 数据蓝 · 利润、链接、分类标签
val BlueAccent      = Color(0xFF2563EB)
val BlueLightBg     = Color(0xFFEFF6FF)
val BorderBlue      = Color(0xFFBFDBFE)

// ── 背景与表面 ──────────────────────────────────────────────
val BgMain          = Color(0xFFF8FAFC)   // 全局底色（极浅中性，无蓝紫色偏）
val BgCard          = Color(0xFFFFFFFF)   // 卡片底色

// ── 文字阶梯 ────────────────────────────────────────────────
val TextPrimary     = Color(0xFF0F172A)   // 深岩灰，高对比度主文字
val TextSecondary   = Color(0xFF475569)   // 次级信息
val TextMuted       = Color(0xFF94A3B8)   // 占位符、辅助说明

// ── 边框与分割线 ────────────────────────────────────────────
val BorderLight     = Color(0xFFE2E8F0)   // 卡片与输入框边框（柔和）
val DividerColor    = Color(0xFFF1F5F9)   // 列表内分割线

// ── 扫码工作台专属 ──────────────────────────────────────────
val ScanBg          = Color(0xFF0B1120)   // 极深扫码背景